@model GodzillaRestaurant.Models.Special
@{
    ViewData["Title"] = "Create Special";
}

<div class="row justify-content-center p-3">
    <div class="col-md-4 form-card">
        <form asp-action="CreateSpecial" class="app-form" enctype="multipart/form-data">
            <h3 class="text-center">Create A Special</h3>
            <partial name="Partial/_SpecialFormPartial.cshtml" />
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
                <a asp-action="Specials" class="btn btn-secondary">Back to list</a>
            </div>
        </form>
    </div>
</div>