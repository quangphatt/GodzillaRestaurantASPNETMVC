@model GodzillaRestaurant.Models.Event
@{
    ViewData["Title"] = "Create Event";
}

<div class="row justify-content-center p-3">
    <div class="col-md-4 form-card">
        <form asp-action="CreateEvent" class="app-form" enctype="multipart/form-data">
            <h3 class="text-center">Create A Event</h3>
            <partial name="Partial/_EventFormPartial.cshtml" />
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
                <a asp-action="Events" class="btn btn-secondary">Back to list</a>
            </div>
        </form>
    </div>
</div>