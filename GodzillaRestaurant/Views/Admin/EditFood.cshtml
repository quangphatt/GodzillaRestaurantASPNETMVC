@model GodzillaRestaurant.Models.Food
@{
    ViewData["Title"] = "Edit Food";
}

<div class="row justify-content-center p-3">
    <div class="col-md-4 form-card">
        <form asp-action="EditFood" class="app-form" enctype="multipart/form-data">
            <h3 class="text-center">Edit Food</h3>
            <partial name="Partial/_FoodFormPartial.cshtml" />
            <div class="form-group">
                <input type="submit" value="Update" class="btn btn-primary" />
                <a asp-action="Menu" class="btn btn-secondary">Back to list</a>
            </div>
        </form>
    </div>
</div>
