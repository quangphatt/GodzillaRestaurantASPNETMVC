@model GodzillaRestaurant.Models.Special
@{
    ViewData["Title"] = "Delete Special";
}

<div class="row justify-content-center p-3">
    <div class="col-md-4 form-card">
        <form asp-action="DeleteSpecial" class="app-form" enctype="multipart/form-data">
            <h5 class="text-center">Are you sure you want to delete this Special?</h5>
            <p><strong>Name: </strong>@Model.Name</p>
            <p><strong>Image:</strong></p>
            @if (Model.Image != null)
            {
                <img src="@Model.Image" class="table-img mb-3" />
            }
            <p><strong>Introduction: </strong>@Model.Introduction</p>
            <p><strong>Description: </strong>@Model.Description</p>
            <p><strong>Detail: </strong>@Model.Detail</p>
            <input type="hidden" asp-for="SpecialId" />
            <a asp-action="Specials" class="btn btn-secondary">Cancel</a>
            <input type="submit" value="Delete" class="btn btn-danger" />
        </form>
    </div>
</div>
