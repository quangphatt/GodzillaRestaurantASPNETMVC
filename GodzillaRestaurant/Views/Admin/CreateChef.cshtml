@model GodzillaRestaurant.Models.Chef
@{
    ViewData["Title"] = "Create Chef";
}

<div class="row justify-content-center p-3">
    <div class="col-md-4 form-card">
        <form asp-action="CreateChef" class="app-form" enctype="multipart/form-data">
            <h3 class="text-center">Create A Chef</h3>
            <partial name="Partial/_ChefFormPartial.cshtml" />
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
                <a asp-action="Chefs" class="btn btn-secondary">Back to list</a>
            </div>
        </form>
    </div>
</div>